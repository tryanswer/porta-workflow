interface:
  display_name: "Porta Workflow"
  short_description: "Build and publish a verified static Porta product"
  default_prompt: "Build this project and publish one verified Static HTML Product through Porta."

policy:
  allow_implicit_invocation: true
